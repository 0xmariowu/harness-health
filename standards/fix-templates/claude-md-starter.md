# {Project Name}

> For global rules, see `~/CLAUDE.md`. This file contains project-specific overrides only.

## Session Checklist

1. If working on {feature area} → read `{path to relevant doc}`
2. Before ending → write experience note

## Workflow

- Branch: `feature/{desc}`, `fix/{desc}` — don't develop on main
- Commit: `{type}: {description}` (feat/fix/refactor/test/docs/chore)
- Tests: `{test command}` must pass before push

## Rules

- {Add project-specific rules here. Each rule should follow: Don't [anti-pattern]. Instead [correct behavior]. Because [reason].}
