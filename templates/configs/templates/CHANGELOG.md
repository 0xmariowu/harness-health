# Changelog

All changes recorded here include a "why" — context future readers need.

## [Unreleased]

<!--
Add entries under this heading until the next release, then cut a new
"## [X.Y.Z] — YYYY-MM-DD" section above it.

Format: - YYYY-MM-DD — <change> because <why it mattered>.

Examples:
  Good: - 2026-04-17 — Added `index` path examples in `INDEX.jsonl` because
          the audit output moved from `docs/reports/` and existing links broke.
  Bad:  - 2026-04-17 — Updated changelog.  (the "why" is missing)
-->
