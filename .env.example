# AgentLint optional environment overrides.
# AgentLint does not auto-load .env files. Export these values or source this file
# in your shell before running the CLI or /al command.

# Multi-project scan root. Defaults to ~/Projects.
PROJECTS_ROOT=~/Projects

# /al command state and report directory. Defaults to ~/.al.
CLAUDE_PLUGIN_DATA=~/.al
